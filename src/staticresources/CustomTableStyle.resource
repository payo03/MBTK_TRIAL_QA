/* 테이블 Padding 조절 */
.stock-table-wrap .slds-table:not(.slds-modal__content .slds-table) th:not(thead th),
.stock-table-wrap .slds-table:not(.slds-modal__content .slds-table) td {
    padding: 0.25rem 0.25rem 0.25rem 0.5rem;
}
